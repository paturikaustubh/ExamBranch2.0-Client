export default function BackupOverview() {
  return (
    <div className="flex flex-col gap-4">
      The main purpose of this page is to make a backup of the table which has
      student’s details (studentInfo). Users can also restore the contents of
      the table by uploading the downloaded backup file.
    </div>
  );
}
